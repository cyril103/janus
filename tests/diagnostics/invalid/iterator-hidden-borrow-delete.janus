import std.array
import std.iterator

def main() : int {
    val values : Array[int] = new Array[int](usize(1))
    val iterator : Iterator[int] = values.iterator()
    delete values
    return int(iterator.count())
}
