const def factorial(value : int) : int {
    if value <= 1 {
        return 1
    } else {
        const previous = factorial(value - 1)
        const result : int = value * previous
        return result
    }
}

const compiled : int = factorial(5)

def main() : int {
    val runtimeInput : int = 5
    println(compiled)
    println(factorial(runtimeInput))
    return 0
}
