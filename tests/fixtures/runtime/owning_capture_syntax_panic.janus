class Resource(val value : int) {
    destructor { println(value) }
}

def main() : int {
    val owner = new Resource(42)
    val once : FnOnce () => Unit = [move owner] () => {
        println(owner.value)
        panic("FnOnce failure")
    }
    defer delete once
    once()
    return 0
}
