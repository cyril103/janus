class Resource(val value : int) {
    destructor {
        println(value)
    }
}

val ready : Resource = new Resource(1)
val failed : Resource = fail()

def fail() : Resource {
    panic("global initialization failed\n")
}

def main() : int {
    return 0
}
