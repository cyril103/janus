class Resource(val id : int) {
    destructor {
        println(id)
    }
}

def invoke(callback : () => Unit) : Unit {
    val resource : Resource = new Resource(30)
    defer delete resource
    callback()
}

def main() : int {
    invoke(() => println(40))
    panic("after callback return\n")
}
