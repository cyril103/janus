import global_config
import other_config

val localCount : int = other_config.visibleCount - 2

def main() : int {
    global_config.importedCount = 3
    return global_config.importedCount + other_config.visibleCount + localCount
}
