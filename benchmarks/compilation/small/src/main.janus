def square(value : int) : int {
    return value * value
}

def main() : int {
    var total : int = 0
    var value : int = 1
    while value <= 20 {
        total = total + square(value)
        value = value + 1
    }
    return total - 2870
}
