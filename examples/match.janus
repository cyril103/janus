import std.option

def main() : int {
    val option : Option[int] = Option.Some[int](42)
    val extracted = match option {
        Some(value) => value,
        None => 0
    }
    return match extracted {
        42 if extracted == 42 => 8,
        _ => 0
    }
}
