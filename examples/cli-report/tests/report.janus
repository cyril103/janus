import std.testing
import report.{userArgumentCount}

/// @test
def removesProgramName() : Unit {
    assertEqual[usize](userArgumentCount(3), 2)
}

/// @test
def acceptsEmptyRuntimeInput() : Unit {
    assertEqual[usize](userArgumentCount(0), 0)
}
