class Point(var x : int, var y : int) {
    destructor {
    }
}

def main() : int {
    val point : Point = new Point(1, 2)
    point.x = 6
    val result : int = point.x
    delete point
    return result
}
