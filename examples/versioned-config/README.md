# Configuration versionnée

`janus run` affiche les thèmes de deux révisions, puis restaure la configuration
sauvegardée. `janus test --fail-if-empty` vérifie remplacement, rollback et branche
indépendante avec des valeurs `Setting` propriétaires non `Copy`.
